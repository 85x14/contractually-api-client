A simple implementation of the Contractually API. This creates a contract, fills it in, and optionally invites a party.

To setup and run:
* `git clone`
* `bundle install`
* `bin/create-contract.rb` - For options, see below
